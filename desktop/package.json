{
  "name": "@agegr/pi-web-desktop",
  "private": true,
  "version": "0.8.0",
  "description": "Desktop shell for Pi Web",
  "author": "Earendil Works",
  "repository": {
    "type": "git",
    "url": "https://github.com/earendil-works/pi.git",
    "directory": "agent/app/desktop"
  },
  "type": "module",
  "license": "MIT",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "package:mac": "electron-builder --mac --config electron-builder.config.ts",
    "package:win": "electron-builder --win --config electron-builder.config.ts",
    "package:linux": "electron-builder --linux --config electron-builder.config.ts"
  },
  "dependencies": {
    "electron-window-state": "^5.0.3"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "electron-vite": "^3.0.0",
    "typescript": "^5"
  }
}
